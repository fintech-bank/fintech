<div class="form-check form-switch form-check-custom form-check-solid mb-5">
    <input class="form-check-input" type="checkbox" name="{{ $name }}" value="{{ $value }}" id="{{ $name }}"
           {{ $check }} />
    <label class="form-check-label" for="{{ $name }}">
        {{ $label }}
    </label>
</div>
